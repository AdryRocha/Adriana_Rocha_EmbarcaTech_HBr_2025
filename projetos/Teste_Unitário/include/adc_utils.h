#ifndef ADC_UTILS_H // ADC_UTILS_H
#define ADC_UTILS_H // Previne inclusão múltipla do arquivo de cabeçalho

#include <stdint.h> // Inclui a biblioteca para tipos de dados inteiros padrão, como uint16_t

float adc_to_celsius(uint16_t adc_val); // Declara a função adc_to_celsius que converte o valor do ADC em temperatura em Celsius

#endif // ADC_UTILS_H
// Fim do arquivo adc_utils.h

